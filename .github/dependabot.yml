version: 2
updates:
# target-branch is mandatory on every ecosystem. Without it Dependabot opens
# PRs against the repository default branch, which skips the PR gate entirely --
# ci-pr.yml only triggers on `pull_request: branches: [dev]` -- and would land
# dependency changes on production without ever passing through integration.
# See workflow/check_workflow_policy.py, which enforces it.
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "type:maintenance"
      - "risk:dependencies"
      - "risk:ci"
# The only Python manifest here is the CI tooling, and it lives in ci/ rather than
# at the root. Pointed at "/" this ecosystem would find nothing and report nothing,
# which reads exactly like a repository with no outdated dependencies.
#
# `risk:ci` as well as `risk:dependencies`, because `ci/requirements-ci.txt` is matched by
# both `ci/**` and its own entry in `risk_paths`. An ecosystem that declares fewer labels
# than its own files require opens pull requests that fail `PR metadata` every week and can
# never be made to pass, since Dependabot relabels nothing after the fact.
  - package-ecosystem: "pip"
    directory: "/ci"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "type:maintenance"
      - "risk:dependencies"
      - "risk:ci"
# The extension's own toolchain. package.json/package-lock.json are matched by
# their own entries in `risk_paths`, so `risk:dependencies` is the only risk
# label these pull requests need.
  - package-ecosystem: "npm"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "type:maintenance"
      - "risk:dependencies"
