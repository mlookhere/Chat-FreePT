pre-commit>=4.6.2
ruff>=0.16.3
mypy>=2.3.1
pytest>=9.1.1
pytest-cov>=7.1.0
lizard>=1.23.0
detect-secrets>=1.5
pip-audit>=2.10.1
build>=1.5.0
# Generates the SBOM that goes into the signed release bundle. The `sbom` group named this
# capability and ran nothing until Issue #92, so the bundle deploy-production verifies had no
# inventory of what it contained.
cyclonedx-bom>=7.3.1
