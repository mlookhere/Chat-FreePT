#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || { cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd; })"
# shellcheck source=scripts/lib/python.sh
. "$ROOT/scripts/lib/python.sh"
exec "$(resolve_python)" "$ROOT/ci/run.py" "$@"
